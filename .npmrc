# Obsidian requires the release tag to match manifest.json's version exactly, with
# no prefix: `1.0.0`, not `v1.0.0`. npm tags as `v<version>` by default, so
# `npm version` would produce a tag the community directory will not accept and
# the release workflow's tag check would reject.
tag-version-prefix=""
