# NPM environment
node_modules/
npm-debug.log
package-lock.json
# Yarn environment
yarn.lock
yarn-error.log

# Build artifacts (published as GitHub release assets, not committed)
# styles.css is source, so we keep it. If you used SCSS, you'd ignore the output css.
main.js
main.js.map

# Compiled test bundles (npm test rebuilds these)
test-build/

# OS generated files
.DS_Store
Thumbs.db

# IDE configuration
.idea/
.vscode/

# Test vaults (may contain plugin data files with API keys / certificates)
Test Vault/
test-vault/
Note Wormhole/
**/.obsidian/

# AI & Agent local state (Private/Local)
# The agent rules and workflow docs live in their own repository:
# https://github.com/VaalRL/obsidian-plugin-agent-kit
.claude/
.gemini/
.mcp.json
CLAUDE.md
gemini.md
claude/
OBSIDIAN_PLUGIN_DEVELOPMENT_RULES.md

# Unrelated side projects — do not ship in the plugin repo
obsidian-mcp-monitor/
Browser Extensions/

# Local Tasks/Docs
todo.md
